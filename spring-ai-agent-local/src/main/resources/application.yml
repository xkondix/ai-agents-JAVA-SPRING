server:
  port: 8084

spring:
  application:
    name: spring-ai-agent-local
  autoconfigure:
    exclude:
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          # llama3.1 (not gemma3): local tools require function calling,
          # which gemma3 does not support — the model would only narrate
          # tool usage in plain text instead of returning tool_calls.
          model: llama3.1:8b
          temperature: 0.3
    # Spring AI 1.1+ — GenAI Semantic Conventions (OTel)
    chat:
      observations:
        include-prompt: true
        include-completion: true

# ── Observability ─────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
    metrics:
      export:
        url: http://localhost:4318/v1/metrics

logging:
  level:
    com.xkondix: DEBUG
    org.springframework.ai: INFO
