server:
  port: 8090

spring:
  application:
    name: raw-agent

# ── Ollama (local model) ──────────────────────────────────────────────────
raw-agent:
  provider: ollama
  ollama:
    base-url: http://localhost:11434
    model: gemma3:4b
    temperature: 0.3
    timeout-seconds: 120

# ── OpenAI / OpenRouter (uncomment and set API key to use) ────────────────
#  provider: openai
#  openai:
#    base-url: https://openrouter.ai/api/v1
#    api-key: ${OPENAI_API_KEY}
#    model: anthropic/claude-sonnet-4-5
#    temperature: 0.3
#    timeout-seconds: 60

# ── Observability ─────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
    metrics:
      export:
        url: http://localhost:4318/v1/metrics

logging:
  level:
    com.xkondix: DEBUG
